# Security-гейт HOL AI Plugin Scanner: требование каталога awesome-codex-plugins
# (score >= 80, без high/critical). Локальный прогон: pip install plugin-scanner
# && plugin-scanner lint .
name: plugin-scan

on:
  push:
    branches: [main]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: hashgraph-online/ai-plugin-scanner-action@d55473348d9b16883ada5aa6db642bc280749365 # v1
