# Ядро детерминированных метрик (skills/humanizer-ru/scripts/: humanizer_metrics,
# scan.py; dev-тулы в scripts/). Этого достаточно для self-test скилла и метрик-MVP.
pymorphy3>=2.0
razdel>=0.5

# Доп-зависимости eval-харнеса (детекторы, LLM-судья) вынесены в
# eval/requirements-eval.txt — они тяжёлые (torch/transformers) и нужны не всем.
